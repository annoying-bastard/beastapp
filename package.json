{
  "name": "beastapp",
  "version": "0.0.1",
  "description": "showcase for cross platform distribution with electron",
  "main": "main.js",
  "scripts": {
    "test": "NODE_ENV=test nyc --reporter=html --reporter=text mocha ./test/unit/index.js",
    "start": "electron .",
    "build": "electron-packager . BEASTMODE --ignore=node_modules/electron-* --asar"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bootstrap": "^3.3.7",
    "electron": "^9.1.0",
    "electron-packager": "^8.6.0",
    "expect.js": "^0.3.1",
    "mocha": "^3.2.0",
    "nyc": "^10.2.0"
  }
}
